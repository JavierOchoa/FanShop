import { createTheme } from "@mui/material";

export const ligthTheme = createTheme({
  palette: {
    mode: "light",
    primary: {
      light: "#fff",
      main: "#fff",
      dark: "#fafafa",
    },
  },
});
